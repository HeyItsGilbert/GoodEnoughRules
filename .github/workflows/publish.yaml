name: Publish Module
on:
  push:
    branches:
      - main
  workflow_dispatch:
permissions: 
  contents: write
jobs:
  Publish:
    uses: HeyItsGilbert/.github/.github/workflows/PublishModule.yml@main
    secrets: inherit
