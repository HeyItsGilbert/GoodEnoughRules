name: CI
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
  issues: write
  checks: write
  pull-requests: write
jobs:
  ci:
    uses: HeyItsGilbert/.github/.github/workflows/ModuleCI.yml@main
